
  # Monetria

  Aplicación de finanzas personales (frontend) construida con React, TypeScript y Vite.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
